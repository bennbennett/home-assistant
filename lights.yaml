# ============================================================================
# DEPRECATED - December 2025
# ============================================================================
# This file used the legacy "platform: template" syntax which is being removed
# in Home Assistant 2026.6.
#
# The outdoor_lights_dimmable template light has been migrated to the modern
# "template:" section syntax in configuration.yaml.
#
# This file is kept for reference only and is no longer included.
# See: configuration.yaml -> template: -> light: section
# ============================================================================

# LEGACY CODE (no longer active):
#
# - platform: template
#   lights:
#     outdoor_lights_dimmable:
#       friendly_name: "Outdoor Lights (Dimmable)"
#       unique_id: outdoor_lights_dimmable_template
#       value_template: "{{ states('light.outdoor_lights') }}"
#       level_template: >
#         {% if state_attr('light.outdoor_lights', 'brightness') %}
#           {{ state_attr('light.outdoor_lights', 'brightness') }}
#         {% elif is_state('light.outdoor_lights', 'on') %}
#           255
#         {% else %}
#           0
#         {% endif %}
#       turn_on:
#         service: light.turn_on
#         target:
#           entity_id: light.outdoor_lights
#         data:
#           brightness: "{{ brightness | default(255) }}"
#       turn_off:
#         service: light.turn_off
#         target:
#           entity_id: light.outdoor_lights
#       set_level:
#         service: light.turn_on
#         target:
#           entity_id: light.outdoor_lights
#         data:
#           brightness: "{{ brightness }}"
